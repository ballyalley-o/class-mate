import express from 'express'
import PARAM from '@routing/param'

const router = express.Router()

/**
 * @path - baseUrl/api/v0.1/cohort/student
 */
router.get('/')
router.post('/')

const studentRouter = router
export default studentRouter
