import { GLOBAL } from '@config'

const RESPONSE = {
  server: {
    name: 'Class Automate',
    status: 'Running',
    API: 'class-server',
    url: GLOBAL.baseUrl,
    port: GLOBAL.port,
    environment: GLOBAL.env,
  },
}

export default RESPONSE
